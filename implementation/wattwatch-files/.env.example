# ── WattWatch Frontend — Environment Variables ─────────────────────────────
# Copy to .env.local and fill in real values. Never commit .env.local!

# Backend URL — set to your Railway deployment URL
VITE_API_URL=http://localhost:8000
